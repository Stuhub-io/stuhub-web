'use client'

import { Listbox, ListboxItem } from "@nextui-org/react"


export const OrgMoreMenu = () => {
    return (
        <Listbox>
            <ListboxItem key="settings">Settings</ListboxItem>
        </Listbox>
    )
}